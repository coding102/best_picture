<p id="notice"><%= notice %></p>
<%= link_to 'New Photo', new_photo_path %>
<h1>Listing Photos</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Image</th>
      <th>Vote</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @photos.each do |u| %>
<%= image_tag u.avatar_url %>
<% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Photo', new_photo_path %>




